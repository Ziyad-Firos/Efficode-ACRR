"""
verify/ — behavioural verification of generated code.

This package exists because generating a plausible refactoring is easy;
proving it still computes the same thing is the hard part. See
sandbox.py's module docstring for why executing submitted code is a
deliberate, documented exception to this project's "nothing submitted is
ever executed" principle, and how that's made safe on every OS.
"""
